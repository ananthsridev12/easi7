{% extends 'partials/base.html.twig' %}

{% block content %}
{% include 'partials/breadcrumbs.html.twig' %}
<section class="page-hero">
  <div class="wrap">
    <span class="eyebrow">Blog</span>
    <h1>{{ page.title }}</h1>
    {% if page.header.summary %}<p class="lede">{{ page.header.summary }}</p>{% endif %}
  </div>
</section>

<section class="block" style="border-bottom:none;">
  <div class="wrap">
    <div class="blog-grid">
      {% for item in page.collection() %}
        {% set cat = item.taxonomy.category[0] ?? 'Strategy' %}
        {% include 'partials/cards/kb-card.html.twig' with { item: item, kind: 'Article', meta: cat ~ ' · ' ~ item.date|date('M Y') } %}
      {% endfor %}
    </div>
  </div>
</section>
{% endblock %}
